/** Next.js config for static export (App Router) */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true
  }
};

module.exports = nextConfig;
module.exports = nextConfig;
